/* 
1. start cmd.exe /k (dentro de este programa ejecuto otro comando) "cd C:\Program File\MongoDB\Server\5.0\bin && mongod.exe --dbpath C:/data/db"
(todo esto para abrir base de datos y los caracteres && para concatenar instrucciones, lo que hay a la derecha es para indicar carpeta donde se guarda la base de datos)
2. start cmd.exe /k "cd C:\wamp64\www\api-rest-node && npm start"
 (Para la api, poner ruta)
3. strart cmd.exe /k "cd (copio ruta del proyecto de React) && npm run dev"
4. Añade MongoDBCompass
5. Añade ruta de Postman 
*/

